__pycache__/
*.pyc
.env
.venv/
venv/
*.log
.pytest_cache/
node_modules/
.DS_Store

# Каталог контуров трасс — сторонние данные (OpenStreetMap/ODbL).
# Намеренно НЕ хранится в репозитории, чтобы не перераспространять чужой
# набор (ТЗ L-02); скачивается при сборке: python scripts/fetch_circuits.py
backend/app/data/circuits.json

# Демо-сессия (backend/app/sources/fixtures/) намеренно ОСТАЁТСЯ в репозитории:
# это наши собственные сгенерированные данные, и так деплой не зависит от
# внешних источников на этапе сборки. Пересоздать: make fixture
